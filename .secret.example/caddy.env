# -----------------------------------------------------------------------------
# 網站位址
# -----------------------------------------------------------------------------
# API 網域名稱 (不含 https://)
# Caddy 會自動為此網域申請 Let's Encrypt SSL 憑證
SITE_ADDRESS=example.com

# -----------------------------------------------------------------------------
# 管理員 Email
# -----------------------------------------------------------------------------
# Let's Encrypt 會使用此 email 發送憑證到期通知
# 請填寫有效的 email 地址
ADMIN_EMAIL=admin@nojteam.com

# =============================================================================
# 注意事項
# =============================================================================
#
# 1. 確保 DNS 已正確設定：
#    api.nojteam.com -> GCE VM 外部 IP
#
# 2. 確保防火牆開放 80 和 443 port：
#    - 80: HTTP (用於 ACME challenge)
#    - 443: HTTPS
#
# 3. 首次啟動時，Caddy 會自動申請 SSL 憑證
#    這可能需要幾分鐘時間
#
# =============================================================================
